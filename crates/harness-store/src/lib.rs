//! `SQLite` persistence, replication, checkpoints. Phase 2 fills this in.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // Replaced with real tests in Phase 2.
        assert_eq!(2 + 2, 4);
    }
}
