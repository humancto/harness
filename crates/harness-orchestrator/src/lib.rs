//! DAG executor, scheduler, fanout controller. Phase 4 fills this in.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // Replaced with real tests in Phase 4.
        assert_eq!(2 + 2, 4);
    }
}
