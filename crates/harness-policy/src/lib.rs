//! Policy engine for capability allow/deny. Phase 3 fills this in.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // Replaced with real tests in Phase 3.
        assert_eq!(2 + 2, 4);
    }
}
