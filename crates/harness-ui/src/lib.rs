//! Embedded web UI assets. Phase 6 fills this in.
//!
//! TODO(decisions): `SvelteKit` vs Leptos is open per PRD §27 Q2.
//! No assets are bundled in Phase 0 — `rust-embed` is not yet a dependency.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // Replaced with real tests in Phase 6.
        assert_eq!(2 + 2, 4);
    }
}
