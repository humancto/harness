[package]
name         = "harness-daemon"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
repository   = { workspace = true }
description  = "Harness daemon — the `harness` binary."

[lints]
workspace = true

[[bin]]
name = "harness"
path = "src/main.rs"

[dependencies]
clap = { workspace = true }
# anyhow will be added back in Phase 1 when subcommands introduce real
# error paths. Phase 0's `main` is infallible.

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
